/**
 * Design system public API
 *
 * Import from `@/lib/design-system` for layout constants and brand color
 * mirrors. Visual tokens for Tailwind live in `src/app/globals.css`.
 */

export {
  type BrandColor,
  brandColors,
  type ContentMaxWidth,
  contentMaxWidth,
  DRAWER_WIDTH_PX,
  HEADER_CONTENT_HEIGHT_PX,
  MOBILE_NAV_BREAKPOINT_PX,
  motionDurationMs,
  TOUCH_TARGET_MIN_PX,
} from './tokens';
